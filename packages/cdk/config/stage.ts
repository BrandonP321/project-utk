export enum APIStage {
  LOCAL = "local",
  DEV = "dev",
  PROD = "prod",
}

export enum WebStage {
  LOCAL = "local",
  DEV = "dev",
  STAGING = "staging",
  PROD = "prod",
}

export enum SharedCdkStage {
  LOCAL = "local",
  DEV = "dev",
  PROD = "prod",
}
